<div class="row mx-auto" style="width: 1200px;">
<div class="container rounded-3 bg-light pt-2 pb-2 ms-5 mt-5">

<h2> Вопрос <%= @question.id %> </h2>

<p class="text-muted pb-2">От user_id: <b> <%= @question.user_id %> </b></p>

<%= render 'question', question: @question %>

<p class="pt-2 pb-2 ms-2 mt-3"><%=link_to "Список всех вопросов", questions_path, class: "btn btn-primary" %></p>

</div>
</div>
